return {
  -- The official GitHub Copilot plugin
  "github/copilot.vim",
  -- Set lazy=false to ensure it loads at startup
  lazy = false,
  -- Optional: Configure keybindings here (Copilot uses <C-l> for accept by default)
  -- You can configure settings like disabling automatic completions or changing the prompt
  -- opts = { auto_suggest = false },
}
