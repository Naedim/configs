# lazygit config

Uses Neovim as file opener in lazygit, with remote-open behavior when lazygit runs inside Neovim.

## Files

- `config.yml`: lazygit editor/open settings.

## Setup

```bash
mkdir -p ~/.config/lazygit
ln -s ~/configs/lazygit/config.yml ~/.config/lazygit/config.yml
```
